import React from 'react';

const styles = {
  wrapper: {
    padding: '30px',
  },
};

const NewTask = () => {
  return (
    <div style={ styles.wrapper }>
      Create a new Task
    </div>
  );
};

export default NewTask;
